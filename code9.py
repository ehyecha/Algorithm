def solution(my_string, queries):
   answer  = ''
   my_string = list(my_string)
   for q in queries:
        a,b = q
        a = int(a)
        b = int(b)
        #문자열 뒤집기 진행하기
      
   return my_string
